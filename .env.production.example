VITE_API_URL=https://your-backend-fqdn/api
VITE_INVOICE_RECONCILE_TEST_MODE=false
# WARNING: VITE_* values are bundled into frontend assets and exposed to browsers.
# Keep protected AI/API secrets on the backend instead of in frontend build args.
